# 2026-04-11 工作日志

## 金币系统接入

- `data.js`：ITEMS 道具价格改为金币单位，PLACEHOLDER_STUDENT 和 INITIAL_STUDENTS 新增 `money` 字段
- `store.js`：
  - `addStudent` 新增 `money: 100` 初始值
  - `buyItem` 改为扣金币（不再扣积分）
  - 新增 `calcDailyIncome(points)` 公式：`max(1, floor(points*0.05) + 10)`
  - 新增 `grantDailyIncome(studentId)` 每日金币发放
- `student.js`：
  - `runDailyPenaltyCheck` 新增日收入发放（每日一次，存 `lastIncome_${id}` 到 localStorage）
  - 商店 UI 改为显示金币 🪙，按钮改用 `student.money` 判断
- `teacher.js`：学生列表改为按姓氏首字编码排序

## 操作记录系统

- `store.js`：
  - state 新增 `auditLog[]` 和 `auditLogRev`
  - 新增 `_sha256(text)` 使用 Web Crypto API 计算 SHA-256
  - 新增 `_logAudit(action, detail, snapshot)` 写日志，持久化到 localStorage
  - 新增 `_loadAuditLog()` 在 init 时从 localStorage 恢复
  - 新增 `revertAudit(entryId)` 支持回退（student_delete/student_update/task_delete/points_change）
  - 新增 `clearAuditLog()` 清空日志
  - `deleteStudent`、`deleteTask`、`grantPoints`、`deductPoints`、`addStudent` 均接入日志
- `admin.js`：新增 `AdminAuditLog` 组件，菜单新增「📋 操作记录」

## 管理员密码加密

- `data.js`：ADMIN_ACCOUNT 改用 `passwordHash` 字段，SHA-256('Lq090825') = `4ad5f311...`
- `store.js`：`login()` 和 `updateAdminAccount()` 改用 `_sha256` 哈希比对/存储
- `login.js`：登录改走 `Store.login()`，删除「admin/admin123」明文提示框


## 本次修复（23:15）\n\n1. **回退功能**：addPoints/spendPoints 原来不写审计日志，导致回退按钮不出现。现已补全快照记录，支持发放积分/扣除积分/消费积分三类回退\n2. **操作记录图例**：AdminAuditLog 页面新增操作类型图例（⭐发放/📉扣除/🎁消费等），标注哪些支持回退\n3. **金币 emoji**：student.js 中所有 🪙 替换为 💵\n4. **学生选择器重构**：\n   - 新增 computed：filteredPickerStudents、groupedPickerStudents、availableLetters、rankByPoints、rankByPetExp\n   - 新增 data：pickerSearch、pickerLandscape（监听 window.resize 更新）、pickerActiveLetter\n   - 新增方法：_getNameInitials（汉字→拼音首字母映射，约300+字覆盖）、scrollToLetter\n   - mounted/beforeUnmount：注册/清理 resize 监听\n   - 模板重写：横屏（≥768px）左侧学生列表（首字母A-Z索引+搜索+3列卡片）+ 右侧排行榜；竖屏搜索框固定顶部（sticky），学生3列，首字母横向滚动\n5. **版权信息**：所有页面底部添加「Made by Qin_zzq · Copyright © 2026」（登录页/学生端/管理端）\n

1. **取消自动恢复登录**：删除 `app.js` mounted 中的 `restoreLoginState()` 调用，每次打开网页均显示登录界面
2. **管理端回退修复**：
   - `student_update` 回退改用 `Vue.set` 确保响应式更新
   - `points_change` 回退补全 `_logAudit` 记录回退操作，加数组引用替换触发响应式
3. **头像删除修复**：`updateAvatar` 传 null 时改用 `Vue.delete(student, 'avatar')` 删除属性，确保头像 UI 正确消失
4. **金币显示加强**：宠物主页积分徽章旁边并排显示金币徽章（金黄色醒目样式）
5. **每日成长修复**：`runDailyPenaltyCheck` 中每日成长奖励从 `grantPoints()` 积分改为直接加 `petExp` 宠物经验，并检测升级


